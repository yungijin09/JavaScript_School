# JavaScript 기초 문법

---

# 자바스크립트 환경 설정

1. 마이크로소프트에서 제공하는 개발 IDE인 비주얼 스튜디오 공식사이트트(https://code.visualstudio.com)에서 다운로드 후 설치 (바탕화면 만들기, path추가)
2. Node.js의 공식 사이트(https://nodejs.org)에 접속해서 Node.js을 다운로드 받아서 설치
3. vs 실행 후 terminal 에서
   - node -v 실행(버전 확인)
   - npm -v 실행(버전 확인)

   -에러 발생시??

4. vs 에 유용한 도구 설치

- html css suppport, HTML snippets(geyao가 제공=> 상황에 따라 선택), JavaScript (ES6) code snippets, ESLint, Prettier- Code formatter(이후 두 가지 설정), live server

- Prettier-Code formatter 설정 : 좌측 하단에 톱니바퀴 모양 선택 -> setting -> default formatter -> Prettier-Code formatter 로 설정 , format on save 를 체크

---

# 1. 자바스크립트란

- **동적인 웹 사이트 제작을 위해 사용**됨

1. 역사 : 1990년대 넷스케이프의 브랜던 아이크(Brenden Eich)가 Mocha라는 이름으로 출시하고, 라이브스크립트로 개발하였다가 최종적으로 자바스크립트가 됨. 그러나 브라우져(넷스케이프, 익스플로러....)에 따라 상이한 스크립트가 나오기 시작하면서 개발이 쉽지 않았으며(if 구문의 남발...).... 스크립트의 표준화에 대한 요구가 생기기 시작함.

_브라우저별로 다르게 발전함_

2. ECMA(European Computer Manufactures Association) 스크립트 : 자바스크립트의 표준작업이 시작된 것으로 1997년 6월 정식으로 채택된 스크립트의 표준. 모던 스크립트는 ES(ECMA script의 약자)6 버전에서 시작한다고 본다.

3. 바닐라 자바스크립트(바닐라 JS) : 어떠한 라이브러리(JQeury..)나 프레임워크(React, Vue, Angular....)를 사용하지 않는 순수 자바스크립트, 가장 빠르고 가벼운 크로스 플랫폼 프레임워크 => 실력 향상을 위한 필수 단계 => 결코 쉽지 않다...=> 그러나 프레임워크나 라이브러리는 모두 바닐라 자바스크립트로 만들어져 있다. => 자신의 실력과 가치를 높이는 강력한 수단이 될 수 있다.

4. 바닐라 자바스크립트 시작하기

- 사이트 : http://vanilla-js.com/
- 시작 : <script src="path/to/vanilla.js"></script>를 html 문서안에 삽입
- 위트 있는 사이트 ^^ : 다운받아도 아무 것도 없다!!!!
- 순수 자바스크립트로 짜면 좋다!!!!

- 최근에는 자바스크립트를 이용하여 웹뿐만 아니라 서버(NodeJS), 데스크톱 프로그램(Electron)도 제작하고 있는 추세
- 최근 [가장 인기있는 언어](http://githut.info/)로 높은 점유율을 차지하고 있음

---

결론(js중심 언어 , reack + Type Script 표준 , 또한 신입은 경쟁력 약화 ai활용 능력 멀티 언어 역량이 커리어 성공의핵심)

_부트 스트랩 bootStrap 중요_

# 2. 자바스크립트 작성 위치

- 외부 파일에서 불러오기 (권장) : ` <script src="./js/01_js_location.js"></script>`
- 파일 안 script 태그에서 직접 사용하기 (수업에서 자주 사용) => `<body>` 중 어디가 가장 좋을까?
- 인라인 자바스크립트 사용
- **CSS의 사용 방식**(외부, style 태그 사용, 인라인 스타일)과 비슷
- 실습 (01_js_location.html)

---

# 3. console 객체의 log 함수

- console에 대한 내장함수를 사용 가능
- 부라우져의 개발자 도구 (f12)를 통해 확인 가능
- 실습 (02_console.html)

---

# 4. 변수 선언자 var, let, const

- 변수란? 프로그램 실행에 필요한 데이터를 담는 기억공간
  - 장점 : 데이터 타입에 따른 선언(int, float, double...)이 필요하지 않다.
  - 단점 : 데이터 타입에 관계없는 변수이기 때문에 개발시 오류 발생이 가능
- 변수 선언자 종류 : var, let, const
- 선언 형식 : 선언자 변수명 + 값;
- 변수 작성 규칙(4가지) : 카멜, 스네이크, 파스칼, 헝가리안
  - 카멜 표기법 : 두 가지 이상의 단어가 합쳐질 경우, 두 번째부터단어의 첫 글자를 대문자로
  - 스네이크 표기법 : 두 개 이상의 단어가 합쳐질 경우 단어 사이에 \_ 삽입
  - 파스칼 표기법 : 모든 단어의 첫 글자를 대문자로
  - 헝가리안 표기법 : 변수명 맨 앞에 데이터 타입에 해당하는 영어단어를 추가하고, 나머지는 카멜 표기법을 따른다.

- **var** 선언자의 특징
  - ES6 이전부터 존재
  - 재선언, 재할당 모두 가능
  - 주로 전역변수에 사용 -> 권장 x (덮어쓰기)

- **let** 선언자의 특징
  - ES6 이후 등장
  - 재선언 불가능, 재할당 가능 -> 권장
  - 동일한 변수명을 다른 곳에서 사용이 불가

- **const** 선언자의 특징
  - ES6 이후 등장
  - 변하지 않는 상수값 -> 변수명 대문자
  - 재선언, 재할당 불가
  - 협업하는 개발자들이 통일해서 사용하는 데이터 선언 등에 사용
    _예)const dateFormat = "YYYY-MM-DD";_

- 실습(03_variable_declaration.html)

---

# 5. 기본 데이터 타입

1. 문자열(String) 객체

- 기호: '또는 '로 데이터를 할당. 가급적 한 가지로 통일하는 것이 좋다.
- 속성 및 함수 : 문자열데이터, 멤버(속성(property, attribute) => 파란색 또는 함수(method) => 보라색)
- 코딩 규칙 : 팀별 코딩 규칙에 따라 문자열 " 또는 '로 지정한다.
- 코딩 중 에러 발생 주의
- 문자열 합치기 : 문자열 + 문자열
- 실습(04_dataType.html)

2. 숫자(Number)형 객체

- 특징
- 속성 및 함수
- 64비트 부동소수점 표현의 문제점(정밀도 오차)
  : 자바스크립트는 자바(int, long, float, double...)등과 달리 메모리는 64비트 부동소수점 형태로 모든 숫자를 관리 -> 약간의 오차가 발생할 수 있다.

- 해결방안
  - 정수로 변환 후 계산
  - 출력 시점에 toFixed() 사용
  - BigInt 사용

- 실습(04_dataType.html)

3. Boolean 객체

- 특징
- 실습(04_dataType.html)

4. undefined 객체

- 특징
  - 정의되지 않은, 값을 할당하지 않은 객체
  - 데이터 타입이자 데이터 값이다.
  - 실행되는 시점, 즉 런타임에 브라우저 엔진이 자동으로 undefined 라는 값을 할당
  - 개발자의 의도보다는 실수인 경우가 많다.
- 실습(04_dataType.html)

5. null 객체

- 특징
- 실습(04_dataType.html)

6. symbol 객체

- 특징

---

# 6. 데이터 타입 - Object

- 정의 : 관련 없는 데이터를 하나로 담을 수 있는 객체
- 사용법 : {}를 사용
- 특징 : 관리가 매우 쉽다.

1. 관련 있는 정보를 하나로 묶어서 관리하고 싶은 경우 사용 : 사람-이름, 전화번호, 이메일주소, 집주소등을 묶고 싶을 경우

2. 키(key : name, phone, email 등 ....) 와 값(value)를 쌍으로 갖는다.

3. 객체의 값에 접근하는 방법(.연산자 사용) : 객체.키

4. 객체의 값을 변경시 객체.키 = 값

5. 객체에 새로운 키와 값을 추가 객체.키 = 값

6. object 의 특정 키에 접근하는 방법
   - 1. 객체.키
   - 2. 객체["키"]

- 실습(05_Object.html)

---

# 7. 데이터 타입 - Array

1. 배열의 정의
   - 하나 이상의 데이터를 담을 수 있다.
   - 데이터의 순서가 있다. => 데이터에 접근하고 수정, 편집이 가능하다.

- []를 이용하여 할당
- 어떤 데이터 타입도 담을 수 있다.
- 어떤 데이터 타입도 함께 담을 수 있다. _ex) let mix = [1, "a", 3, true. {}, []]_

2. 배열의 순서
   - 순번 : 인덱스(index)
   - 인덱스는 0부터 1씩 증가한다.
   - 인덱스를 이용하여 데이터에 접근 및 수정이 가능하다.

3. 배열의 활용
   - 다양한 내장함수(push(), find(), concat()....)를 사용할 수 있다.
   - object와 함께 활용이 가능(배열 안에 object가 있는 형태)

- 실습(06_Array.html)

---

# 8. 64비트 부동소수점

1. 에러를 발생시켜 보자.
2. 에러의 발생을 미연에 방지하려면?
3. 자바스크립트에서 사용하는 가장 큰 수와 작은 수는?

- 실습

---

# 9. 연산자

1. 할당 연산자

- 특징 : 오른쪽 피연산자의 값을 왼쪽 변수에 할당
- 종류 : =, 복합 할당 연산자(+=, -=, \*=, /=, %=)
- 실습(08_operator.html)

2. 비교 연산자

- 특징 : 값(타입) 비교
- 종류 : (==, ===, !=, !==, >, >=, <, <=)
- 값과 타입의 비교 : ===
- 실습(08_operator.html)

3. 산술 연산자

- 특징 : 증가(++), 감소(--), 단항 부정(-), 숫자화(+)
- 종류 : +, -, \*, /, %(나머지), \*\*(제곱근), ++, --
- 실습(08_operator.html)

3-1. 단항 부정 연산자

- 특징 : 부호가 바뀜
- 실습(08_operator.html)

3-2. 숫자화 연산자

- 특징 : 숫자가 아닌 문자, boolean 값을 숫자로 변환
- 실습(08_operator.html)

4. 논리 연산자

- 특징 : 조건을 여러 가지 만족시키는지 알아보는 연산자
- 종류 : &&(둘 다 true일 경우 true), ||(둘 중 하나만 true이면 true)
- 실습(08_operator.html)

5. 문자열 관련 연산자

- 특징 : 합치기
- 종류 : +
- 실습(08_operator.html)

6. 조건(삼항) 연산자

- 특징
  - 조건 ? true 일 때 할당할 값 : false일 때 할당할 값
  - 중첩해서 사용이 가능
- 실습(08_operator.html)

---

# 10. 조건문

- 특징
  - 가장 중요한 구문 중 하나
  - 프로그램 => 조건문의 집합
  - if : 만일 ~라면...
  - 작성 시 순서가 효율성에 영향을 미친다.

1. 조건문 if

- if
  : if(조건식1){
  조건을 만족할 경우 실행할 코드
  }
- if else
  : if(조건식1){
  ~~
  }
  else{
  조건을 만족하지 않을 경우 실행할 코드
  }

- if else if else if .... else
  if(조건식1){
  ~~
  }
  else if(조건식2){
  조건식 1을 만족하지 않고 조건식 2를 만족하는 경우
  }
- 항상 false 인 값 :
- 실습(09_if.html)

2. 조건문 switch

- 특징
  - 상수값만 비교
  - 실무에서 거의 사용하지 않는다.
- 장점: if 문을 사용하면 모든 조건을 비교하면서 내려오지만 switch문의 경우 값을 비교해서 바로 찾아간다. 즉 성능 향상 장점이 있다 => 컴퓨터의 성능이 워낙 좋으므로 크게 신경 쓰지 않는다.
- 단점 : 오히려 코드가 길어지는 단점이 있다.
- 실습(10_switch.html)

- 참고: **Date**객체

---

# 11. 반복문

- 특징 : 특정 코드 블록을 정해진 횟수만큼 반복해서 실행하게 하는 문장

1. for loop

- 특징 : 배열에서 가장 많이 사용
- 동작 원리
  - for(statement1; statement2; statement3){
    조건절이 참인 동안 실행할 코드
    }
  - statement1 : 코드 블록을 실행하기 전에 무조건 한 번 실행하는 초기화 문장
  - statement2 : 코드 블록을 실행시킬 조건문
  - statement3 : 코드 블록 실행한 후 실행되는 횟수 증가 코드
- 실습(11_for_loop.html)

- for 과 break : break를 만나면 반복문 블럭을 중단하고 블록을 빠져나온다.
- for 과 continue : continue를 만나면 반복문을 다시 실행

1-1. for ~ in

- 특징 : 배열, object에서 많이 사용(주로 객체)
- 실습(12_for_in.html)

1-2. for ~ of

- 특징 :
  - 배열에서 많이 사용
  - 일반 string 문자열에서도 사용 가능하며 문자열을 분해한다.
- 실습(13_for_of.html)

1-3. for ~ each

- 특징: 배열에서 많이 사용하며 매개변수로 함수를 갖는다.
- 실습(14_for_each.html)

2.  while loop

- 특징
- 실습(15_while.html)

3.  do - while

- 특징
- 실습

4. 반복문의 활용

---

# 12. 함수

- 정의
  - 반복적인 기능, 재사용해야 할 코드들의 모임
  - 재사용 가능한 구조로 만든 코드들의 집합 => 중복 작업을 피할 수 있고, 효율성을 높일 수 있다.
- 특징
  - 앞으로 변경이 가능한 값을 가진 경우 적합 
  - 팀 프로젝트의 경우, common js 파일을 만들어 사용하면 수정과 적용에 매우 우수한 효율성을 보인다.
  - 

- 브라우져 엔진이 메모리에 올려놓는 순서 & 코드 순서
   1. 선언함수 -> 로딩
   2. var -> 로딩
1. 함수 선언식 방식

- 선언 : function 함수명(매개변수1, 매개변수2 ...){
           .... 코드 블록 ....
           (return ...)
           *함수 안에서 생성된 변수, 매개변수는 var 타입의 변수여도 함수 내에서만 사용 가능*
        }
        
- 사용 : 선언하고 호출하여 실행
- 특징 : 함수 선언식 방식은 선언하는 위치에 관계없이 브러우져 엔진이 가장 먼저 메모리에 로딩한다. 따라서 코드 순서상 선언 전에 호출하여도 무방
- 실습(16_function.html)
- common.js 파일의 활용 및 장점

2. 함수 표현식 방식

- 선언 : 변수명이 함수명처럼 사용
- 사용 : 변수 사용과 비슷한 방식으로 변수에 함수를 할당해서 사용
- 유의할 사항
  1. 함수 표현식은 반드시 선언 후 호촐 가능
  2. 선언식은 선언부 아래에 있으므로 가독성의 문제가 비교적 적은데 반해 함수 표현식 방식은 가독성의 문제가 발생
- 실습(16_function.html)

3. Function 생성자 함수
ex) Student s1 = new Student() // java
Student -> 클래스 이름
s1 -> 인스턴스 객체(변수명) = reference
new -> 새로운 메모리 할당
Student() -> 생성자 
- 선언
- 사용
- 언제 유용하게 사용할까?
- 실습(16_function.html)

4. 화살표 함수
  - 

5. 함수 자체가 파라미터로 전달되는 함수

- forEach() 실습
